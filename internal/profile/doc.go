// Package profile defines named persisted skill selections.
//
// Profiles keep only lightweight skill identities so the saved selection stays
// stable even when catalog metadata changes.
package profile
